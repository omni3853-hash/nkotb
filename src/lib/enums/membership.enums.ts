export enum MembershipStatus {
    ACTIVE = "ACTIVE",
    EXPIRED = "EXPIRED",
    SUSPENDED = "SUSPENDED",
    PENDING = "PENDING",
    CANCELED = "CANCELED",
}

export enum BillingPeriod {
    MONTH = "MONTH",
    YEAR = "YEAR",
    CUSTOM = "CUSTOM",
}
